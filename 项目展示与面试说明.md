# Matrix AI 数据工具箱｜项目展示与面试说明

## 一页简历精简描述

**Matrix AI 数据工具箱｜Python / Flask / Java JDBC / openpyxl / LLM API**

- 基于实习工作中的数据查询与核对需求，开发本地 Web 数据工具，整合只读 SQL 查询、全库表行数统计、SQL 逻辑排查、库表数据比对和文件转换能力；
- 封装 Java/JDBC 查询模块，支持运行时配置 JDBC URL、驱动类和驱动 JAR，连接地址与账号密码仅保存在本机进程内存中；
- 实现自然语言生成只读 SQL、基于静态解析证据的 SQL 语义差异推理和执行前安全检查，区分直接证据、影响假设与验证步骤，模型结果需人工确认后才能进入查询页；
- 实现 Excel/CSV 按主键差异比较、重复键组内匹配，以及 Excel 转 INSERT SQL、PDF、Word 和文本/图片转 Excel；
- 完成项目脱敏、依赖整理、Windows 一键启动、接口自检与本地部署验证，形成可独立演示的交付版本。

> 不要写“PDF/Word 转 Excel”。当前代码实现的是 Excel 转 PDF/Word；图片转 Excel 已支持 Windows 10/11 与 macOS 的本机 OCR，但识别结果必须人工核对。

## 面试中的 90 秒介绍

这个项目来自我实习时遇到的真实问题：数据库查询、表数据量统计、SQL 排查、Excel 数据核对和格式转换分散在不同操作里，重复工作比较多。我把这些流程整合成了一个只在本机运行的 Web 工具。

后端使用 Python Flask，数据库层通过 Java/JDBC 适配驱动，Excel 处理使用 openpyxl。为了避免公司账号进入代码，我把连接方式改成运行时前端配置，密码只保存在进程内存，并对所有数据库调用统一做只读检查。

在此基础上，我增加了 AI SQL 助手：用户提供脱敏表结构和查询需求，模型生成单条只读 SQL；SQL 排查则先由本地解析器提取 JOIN、过滤条件、字段和规则风险，再让模型基于这些证据解释语义影响、提出待验证假设。模型建议的验证 SQL 会再次经过只读检查，并且不会自动执行。

项目使用 Qoder 辅助完成部分代码生成和调试。我主要负责需求拆解、功能组合、运行验证、安全脱敏、问题修复和交付整理，并能够解释数据库连接、SQL 安全检查和文件比较的实现流程。

## 五分钟演示顺序

1. 打开首页，说明工具只监听 `127.0.0.1`，数据库和 AI 密钥均不固化到仓库。
2. 在 AI SQL 助手点击“填入脱敏示例”，展示自然语言需求、表结构和只读安全检查。
3. 展示 SQL 查询、表行数统计和 SQL 排查工作台三个核心页面，不需要真实执行公司数据库。
4. 使用两份自建 Excel/CSV 演示主键差异比较，再展示 Excel 转 INSERT。
5. 最后说明边界：AI 与 OCR 结果要人工复核；大结果会明确标注抽样、预览和导出上限，全量 SQL 核对需要业务时间字段。

## 面试官追问时的真实回答

**是不是全部手写？**

不是。项目使用 Qoder 辅助生成和修改代码，我负责把真实需求拆成模块，检查生成结果、调试运行、补安全限制、做脱敏和交付验证。关键流程我能解释，也能继续修改。

**为什么数据库层用 Java/JDBC？**

目标数据库通过对应 JDBC 驱动接入。Python 负责页面和业务编排，Java 小查询器负责加载驱动并执行查询，这样连接信息和 SQL 可以通过标准输入传递，不需要固化专用驱动逻辑。

**如何避免 AI 误操作数据库？**

AI 只负责生成文本，不直接拿数据库执行权限。生成 SQL 必须经过本地只读检查并由用户手动放入查询页；数据库调用层还会再次检查，底层使用 `executeQuery()`，实际账号也应配置为只读最小权限。

**项目还可以怎么改进？**

可以补充更多数据库方言测试、查询取消、更完整的审计日志、对比任务分页，以及在公司允许的前提下接入内部模型和表结构检索。现阶段不会把这些未实现项写成已经完成。
