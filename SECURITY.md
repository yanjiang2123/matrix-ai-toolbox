# 安全与脱敏说明

## 本版本的处理

- 未包含原项目的 `dist/`、`.selftest/`、`__MACOSX/`、运行日志、上传目录或产物目录。
- 未包含原有 `config.json`；仓库只保留重新创建的空白 `config.example.json`。
- 已移除组织名称、真实环境名称、连接地址、账号密码、绝对用户路径、真实表名示例和内部知识文档引用。
- JDBC 驱动类改为可配置，不再在源码中硬编码专用驱动类。
- 数据库密码由前端提交后仅保存在当前 Python 进程内存中，不写回磁盘、不通过状态接口返回。
- JDBC URL、SQL、用户名和密码通过标准输入传给 Java 查询器，不出现在进程命令行参数或环境变量中。
- 后端拒绝在 JDBC URL 中携带常见用户名或密码参数，避免凭据随 URL 进入进程参数。
- 所有查询、上传、导出和设置类 API 都要求当前页面的随机会话令牌。
- AI 服务地址、模型名和 API Key 仅保存在当前进程内存，不写入配置文件、不通过状态接口回传 Key。
- 外部 AI 服务默认要求 HTTPS；非本机 HTTP 服务只有经使用者明确勾选后才允许连接。
- AI 生成的 SQL 不会自动执行，且所有 JDBC 查询入口统一执行多语句与写操作检查。
- AI SQL 对比会发送使用者主动提交的两段 SQL、业务背景和本地静态解析摘要；发送前页面会再次提示脱敏。
- AI 建议的验证 SQL 必须通过本地只读检查；未通过的 SQL 不返回到可复制区域，通过的 SQL 也只允许手动转入查询页。

## AI 数据边界

- 只有使用者主动点击“生成”或“分析”时，当前输入框中的内容才会发送给所配置的模型服务。
- 工具不会自动把查询结果、其他页面的比对明细或数据库密码发送给 AI。
- 使用云端模型前，应按组织规定对表名、字段名、报错、统计结果和业务描述进行脱敏。
- 本地只读检查不能判断业务口径是否正确，也不能替代模型服务的数据保护协议和数据库权限控制。

## 发布前仍需执行

1. 轮换原项目中曾经出现过的数据库账号和密码；“从新副本中删除”不能撤销旧凭据的暴露。
2. 从全新的 Git 仓库开始提交，不要把可能包含旧密钥的历史直接推送到远端。
3. 不要复制原来的 `dist/`；必须使用本脱敏源码和空白配置重新构建。
4. 再次扫描输出包中的域名、手机号、邮箱、用户目录、密码字段和业务文件。
5. 数据库账号必须使用只读、最小权限账号，并限制网络访问范围。

## 不应提交或分发

```text
dist/
build/
.selftest/
data/
uploads/
~/.matrix_toolbox/
config.local.json
config.json
table_pairs.json
数据库驱动 JAR
JDK
任何导出的 xlsx/csv/pdf/docx/sql/log
```
